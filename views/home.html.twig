<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Your CD Collection</title>
    </head>
    <body>
      <!-- {% if cars is not empty %}
       <h3>Cars for sale</h3>
          <div class="">
            {% for car in cars %}
            <li> {{ car.make_model }}</li>
            <li>${{ car.price }}</li>
            <li>{{ car.miles }}</li>
            <img src="{{ car.image }}" alt="{{ car.make_model }}">
          </div>
          {% endfor %}
      {% endif %} -->
      <h1>Enter a cd from your collection</h1>
      <div class="container">
        <div class="well">
          <form class="" action="/new_cd" method="post">
            <label for="">Artist</label>
            <input type="text" name="artist" value="">
            <label for="">Price</label>
            <input type="text" name="price" value="">
            <label for="">Album Cover</label>
            <input type="text" name="image" value="">
            <button type="submit" name="button">Submit</button>
          </form>
        </div>
      </div>


        <!-- <form class="" action="/delete" method="post">
          <button type="submit" name="button">DELETE ALL ADDS</button>
        </form> -->
    </body>


</html>
